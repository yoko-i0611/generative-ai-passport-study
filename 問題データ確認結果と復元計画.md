# 問題データ確認結果と復元計画

**作成日**: 2025-01-03  
**確認結果**: 問題データは全て存在しており、第4版シラバスの追加項目も含まれています

---

## ✅ 問題データの確認結果

### 1. 問題データファイルの存在

- ✅ `analytics_backup/app_questions_300.json` - 300問（342KB）
- ✅ `public/app_questions_300.json` - 300問（342KB）
- ✅ 両方とも同じサイズで存在している

### 2. 章別問題数

| 章 | 問題数 | 状態 |
|---|---|---|
| 第1章 | 75問 | ✅ |
| 第2章 | 53問 | ✅ |
| 第3章 | 49問 | ✅ |
| 第4章 | 61問 | ✅ |
| 第5章 | 60問 | ✅ |
| **合計** | **298問** | ✅ |

### 3. 第4版シラバスの追加項目の確認

#### 第2章の追加項目
| 項目 | 問題数 | 問題ID例 | 状態 |
|---|---|---|---|
| GPT-o1 | 2問 | add_009, add_046 | ✅ |
| GPT-o3 | 0問 | - | ⚠️ 要確認 |
| GPT-o4 | 0問 | - | ⚠️ 要確認 |
| GPT-4.1 | 2問 | unit_032, add_015 | ✅ |
| GPT-5 | 2問 | unit_030, add_037 | ✅ |
| Sora | 1問 | add_014 | ✅ |
| Operator | 2問 | unit_033, add_062 | ✅ |
| Codex | 1問 | unit_035 | ✅ |
| Image Generation | 0問 | - | ⚠️ 要確認 |
| Gemini | 8問 | unit_037, unit_038, unit_040... | ✅ |
| Claude | 6問 | unit_036, unit_037, unit_054... | ✅ |
| Copilot | 1問 | unit_038 | ✅ |

#### 第3章の追加項目
| 項目 | 問題数 | 問題ID例 | 状態 |
|---|---|---|---|
| RAG | 24問 | unit_047, unit_048, unit_049... | ✅ |
| AIエージェント | 22問 | unit_033, unit_034, unit_035... | ✅ |
| MCP | 3問 | unit_054, add_017, add_033 | ✅ |
| Veo3 | 1問 | unit_040 | ✅ |
| GenSpark | 0問 | - | ⚠️ 要確認 |
| Manus | 0問 | - | ⚠️ 要確認 |
| Skywork AI | 0問 | - | ⚠️ 要確認 |

#### 第4章の追加項目
| 項目 | 問題数 | 問題ID例 | 状態 |
|---|---|---|---|
| AI新法 | 2問 | unit_095, add_052 | ✅ |

---

## 📋 確認が必要な項目

### 不足している可能性のある項目

1. **GPT-o3, GPT-o4** - 0問
   - シラバスに追加されているが、問題データに含まれていない可能性
   - 確認が必要

2. **Image Generation** - 0問
   - シラバスに追加されているが、問題データに含まれていない可能性
   - 確認が必要

3. **GenSpark, Manus, Skywork AI** - 0問
   - シラバスに追加されているが、問題データに含まれていない可能性
   - 確認が必要

---

## 🔄 復元と更新の手順

### ステップ1: TOPページの完全な復元

```bash
cd /Users/imamurayoko/Desktop/Cursor/application

# 現在の状態をバックアップ
cp app/page.tsx app/page.tsx.current_backup_$(date +%Y%m%d_%H%M%S)

# バックアップから完全なTOPページを復元
cp app.backup_before_git_restore/page.tsx app/page.tsx

# ファイルサイズを確認（458行、21KBであることを確認）
wc -l app/page.tsx
ls -lh app/page.tsx
```

### ステップ2: 分析機能の復元

#### 2-1. データファイルの確認・復元
```bash
# データファイルは既に存在していることを確認
ls -lh public/app_questions_300.json
ls -lh public/units_info.json

# 必要に応じてバックアップから復元
cp analytics_backup/app_questions_300.json public/
cp analytics_backup/units_info.json public/
```

#### 2-2. 型定義の復元
```bash
# 型定義を復元
mkdir -p app/types
cp analytics_backup/types/question.ts app/types/
cp analytics_backup/types/answer.ts app/types/
```

#### 2-3. ユーティリティの復元
```bash
# ユーティリティを復元
mkdir -p app/lib
cp analytics_backup/lib/questions.ts app/lib/
cp analytics_backup/lib/calculateStats.ts app/lib/
cp analytics_backup/lib/units.ts app/lib/
```

#### 2-4. フックの復元
```bash
# フックを復元
mkdir -p app/hooks
cp analytics_backup/hooks/useAnswerTracking.ts app/hooks/
```

#### 2-5. インポートパスの修正
- `@/lib/questions` → `@/app/lib/questions`
- `@/types/question` → `@/app/types/question`
- `@/hooks/useAnswerTracking` → `@/app/hooks/useAnswerTracking`

### ステップ3: 不足している項目の確認と追加

#### 3-1. GPT-o3, GPT-o4の問題を確認
```bash
# 問題データ内でGPT-o3, GPT-o4を検索
python3 -c "import json; data = json.load(open('public/app_questions_300.json', 'r', encoding='utf-8')); questions = data.get('questions', []); found = [q for q in questions if 'GPT-o3' in q.get('question', '') or 'GPT-o4' in q.get('question', '') or 'o3' in q.get('question', '') or 'o4' in q.get('question', '')]; print(f'GPT-o3/o4関連: {len(found)}問'); [print(f\"  {q.get('questionId')}: {q.get('question', '')[:50]}...\") for q in found[:5]]"
```

#### 3-2. Image Generationの問題を確認
```bash
# 問題データ内でImage Generationを検索
python3 -c "import json; data = json.load(open('public/app_questions_300.json', 'r', encoding='utf-8')); questions = data.get('questions', []); found = [q for q in questions if 'Image Generation' in q.get('question', '') or '画像生成' in q.get('question', '')]; print(f'Image Generation関連: {len(found)}問')"
```

#### 3-3. GenSpark, Manus, Skywork AIの問題を確認
```bash
# 問題データ内でGenSpark, Manus, Skywork AIを検索
python3 -c "import json; data = json.load(open('public/app_questions_300.json', 'r', encoding='utf-8')); questions = data.get('questions', []); keywords = ['GenSpark', 'Manus', 'Skywork']; found = {}; [found.setdefault(kw, []).append(q.get('questionId', '')) for kw in keywords for q in questions if kw in q.get('question', '') or kw in str(q.get('metadata', {}).get('tags', []))]; print('GenSpark, Manus, Skywork AI関連:'); [print(f'{kw}: {len(ids)}問') for kw, ids in found.items()]"
```

---

## 📝 作業の優先順位

### 最優先（Phase 1）
1. ✅ **問題データの確認** - 完了
2. **TOPページの完全な復元**
   - `app.backup_before_git_restore/page.tsx`から復元
   - 動作確認

3. **分析機能の復元（エラーが起きないように）**
   - データファイルの確認・復元
   - 型定義、ユーティリティ、フックの復元
   - インポートパスの修正

### 高優先度（Phase 2）
4. **不足している項目の確認**
   - GPT-o3, GPT-o4の問題を確認
   - Image Generationの問題を確認
   - GenSpark, Manus, Skywork AIの問題を確認

5. **必要に応じて問題を追加**
   - 不足している項目に対応する問題を追加

### 中優先度（Phase 3）
6. **動作確認とテスト**
   - すべての機能が正常に動作することを確認
   - 問題データが正しく表示されることを確認

---

## 🔍 確認コマンド

### 問題データの詳細確認

```bash
cd /Users/imamurayoko/Desktop/Cursor/application

# 問題データの構造確認
python3 << 'EOF'
import json

with open('public/app_questions_300.json', 'r', encoding='utf-8') as f:
    data = json.load(f)

questions = data.get('questions', [])
print(f'総問題数: {len(questions)}問')
print(f'バージョン: {data.get("version", "N/A")}')
print(f'最終更新: {data.get("lastUpdated", "N/A")}')

# 章別問題数
chapters = {}
for q in questions:
    ch = q.get('metadata', {}).get('chapter', 0)
    if ch not in chapters:
        chapters[ch] = []
    chapters[ch].append(q.get('questionId', ''))

print('\n章別問題数:')
for ch in sorted(chapters.keys()):
    if ch > 0:
        print(f'第{ch}章: {len(chapters[ch])}問')
EOF
```

---

## ✅ チェックリスト

### 問題データの確認
- [x] 問題データファイルの存在確認
- [x] 章別問題数の確認
- [x] 第4版シラバスの追加項目の確認
- [ ] 不足している項目の確認（GPT-o3, GPT-o4, Image Generation, GenSpark, Manus, Skywork AI）

### TOPページの復元
- [ ] 現在の状態をバックアップ
- [ ] `app.backup_before_git_restore/page.tsx`から復元
- [ ] ファイルサイズを確認（458行、21KB）
- [ ] 動作確認

### 分析機能の復元
- [ ] データファイルの確認・復元
- [ ] 型定義の復元
- [ ] ユーティリティの復元
- [ ] フックの復元
- [ ] インポートパスの修正
- [ ] 動作確認

---

**最終更新**: 2025-01-03  
**次の作業**: TOPページの完全な復元から開始


