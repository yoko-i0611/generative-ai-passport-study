# 問題データの整理と確認

**作成日**: 2025-01-03  
**確認結果**: 2種類の問題データが存在していることを確認

---

## ✅ 問題データの種類

### 1. 全300問の問題演習データ

**用途**: TOPページの「問題演習に挑戦」ボタンからアクセスする問題演習

**ファイル**:
- `public/app_questions_300.json` - 300問（342KB）
- `analytics_backup/app_questions_300.json` - バックアップ（342KB）

**内容**:
- 章別問題数: 第1章75問、第2章53問、第3章49問、第4章61問、第5章60問
- 第4版シラバスの追加項目を含む
- 問題ID形式: `unit_001`, `add_001` など

**使用箇所**:
- `/quiz` ページ（`app/quiz/page.tsx`）
- 問題演習機能

---

### 2. ユニットごとの確認テストデータ

**用途**: 各章の学習ページで、ユニットごとに確認テストを行う

**ファイル**:
- `~/Desktop/KindleScreenshots/unit_quizzes_all.json` - ユニットごとの確認テスト（127KB）
- `~/Desktop/KindleScreenshots/backup_20251231_144906/unit_quizzes_all.json` - バックアップ

**内容**:
- 22ユニットの確認テスト
- 各ユニットに3-7問程度の問題
- 章別に整理されている

**使用箇所**:
- `/courses/chapter1` ページ（`app/courses/chapter1/page.tsx`）
- `/courses/chapter2` ページ（`app/courses/chapter2/page.tsx`）
- `/courses/chapter3` ページ（`app/courses/chapter3/page.tsx`）
- `/courses/chapter4` ページ（`app/courses/chapter4/page.tsx`）
- `/courses/chapter5` ページ（`app/courses/chapter5/page.tsx`）
- `UnitQuizModal` コンポーネント（`app/components/UnitQuizModal.tsx`）

---

## 📊 データ構造の比較

### 全300問の問題演習データ（app_questions_300.json）

```json
{
  "version": "1.0.0",
  "totalQuestions": 300,
  "lastUpdated": "2025-01-15T00:00:00Z",
  "description": "生成AIパスポート試験対策問題集（300問）",
  "questions": [
    {
      "questionId": "unit_001",
      "question": "...",
      "options": [...],
      "correctAnswer": "...",
      "explanation": "...",
      "metadata": {
        "chapter": 1,
        "category": "第1章 AI（人工知能）",
        "source": "unit",
        "tags": ["AI基礎"],
        "difficulty": "medium",
        "unitId": "unit01"
      }
    }
  ]
}
```

### ユニットごとの確認テストデータ（unit_quizzes_all.json）

```json
[
  {
    "chapter": 1,
    "sectionId": "unit01-ai-definition-history",
    "sectionTitle": "AIの定義と歴史",
    "questions": [
      {
        "question": "...",
        "options": [...],
        "correctAnswer": "...",
        "explanation": "..."
      }
    ]
  }
]
```

---

## 🔍 確認が必要な項目

### 1. ユニットごとの確認テストデータの配置

**現在の状態**:
- `~/Desktop/KindleScreenshots/unit_quizzes_all.json` - 存在
- `public/` ディレクトリには配置されていない可能性

**必要な作業**:
- `unit_quizzes_all.json` を `public/` ディレクトリに配置する必要があるか確認
- または、アプリケーション内で直接読み込む構造になっているか確認

### 2. データの整合性

**確認項目**:
- ユニットごとの確認テストデータが第4版シラバスに準拠しているか
- 全300問の問題演習データとユニットごとの確認テストデータの整合性
- 問題IDの対応関係

### 3. データの使用方法

**確認項目**:
- 各章のページ（`app/courses/chapter*/page.tsx`）でユニットごとの確認テストデータをどのように読み込んでいるか
- `UnitQuizModal` コンポーネントでどのようにデータを使用しているか

---

## 📝 復元と更新の手順

### ステップ1: データファイルの確認

```bash
cd /Users/imamurayoko/Desktop/Cursor/application

# 全300問の問題演習データの確認
ls -lh public/app_questions_300.json
ls -lh analytics_backup/app_questions_300.json

# ユニットごとの確認テストデータの確認
ls -lh ~/Desktop/KindleScreenshots/unit_quizzes_all.json

# ユニット情報の確認
ls -lh public/units_info.json
ls -lh analytics_backup/units_info.json
```

### ステップ2: ユニットごとの確認テストデータの配置

```bash
# ユニットごとの確認テストデータをpublic/に配置（必要に応じて）
cp ~/Desktop/KindleScreenshots/unit_quizzes_all.json public/
```

### ステップ3: データの整合性確認

```bash
# ユニットごとの確認テストデータの内容確認
python3 << 'EOF'
import json

# ユニットごとの確認テストデータを読み込み
with open('/Users/imamurayoko/Desktop/KindleScreenshots/unit_quizzes_all.json', 'r', encoding='utf-8') as f:
    unit_quizzes = json.load(f)

print(f'ユニット数: {len(unit_quizzes)}ユニット')
total_questions = sum(len(s.get('questions', [])) for s in unit_quizzes)
print(f'総問題数: {total_questions}問')

# 章別の集計
chapters = {}
for section in unit_quizzes:
    ch = section.get('chapter', 0)
    if ch not in chapters:
        chapters[ch] = {'units': 0, 'questions': 0}
    chapters[ch]['units'] += 1
    chapters[ch]['questions'] += len(section.get('questions', []))

print('\n章別ユニット数と問題数:')
for ch in sorted(chapters.keys()):
    if ch > 0:
        print(f'第{ch}章: {chapters[ch]["units"]}ユニット、{chapters[ch]["questions"]}問')
EOF
```

### ステップ4: 第4版シラバス準拠の確認

```bash
# ユニットごとの確認テストデータが第4版シラバスの追加項目を含んでいるか確認
python3 << 'EOF'
import json

# ユニットごとの確認テストデータを読み込み
with open('/Users/imamurayoko/Desktop/KindleScreenshots/unit_quizzes_all.json', 'r', encoding='utf-8') as f:
    unit_quizzes = json.load(f)

# 第4版シラバスの追加項目
keywords = [
    'GPT-o1', 'GPT-o3', 'GPT-o4', 'GPT-4.1', 'GPT-5',
    'Sora', 'Operator', 'Codex', 'Image Generation',
    'Gemini', 'Claude', 'Copilot',
    'RAG', 'AIエージェント', 'MCP', 'Veo3',
    'GenSpark', 'Manus', 'Skywork AI',
    'AI新法'
]

found = {}
for section in unit_quizzes:
    for question in section.get('questions', []):
        question_text = question.get('question', '')
        for kw in keywords:
            if kw in question_text:
                if kw not in found:
                    found[kw] = []
                found[kw].append(section.get('sectionId', ''))

print('第4版シラバスの追加項目の確認（ユニットごとの確認テスト）:')
for kw in keywords:
    if kw in found:
        print(f'{kw}: {len(found[kw])}問（{len(set(found[kw]))}ユニット）')
    else:
        print(f'{kw}: 0問')
EOF
```

---

## ✅ チェックリスト

### データファイルの確認
- [x] 全300問の問題演習データの存在確認
- [x] ユニットごとの確認テストデータの存在確認
- [ ] ユニットごとの確認テストデータの配置確認（public/に配置されているか）
- [ ] ユニット情報（units_info.json）の確認

### データの整合性確認
- [ ] ユニットごとの確認テストデータの章別集計
- [ ] 全300問の問題演習データとユニットごとの確認テストデータの整合性
- [ ] 第4版シラバス準拠の確認

### データの使用方法確認
- [ ] 各章のページでユニットごとの確認テストデータをどのように読み込んでいるか
- [ ] `UnitQuizModal` コンポーネントでどのようにデータを使用しているか

---

**最終更新**: 2025-01-03  
**次の作業**: ユニットごとの確認テストデータの配置と整合性確認


